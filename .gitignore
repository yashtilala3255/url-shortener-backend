# This is a comment - lines starting with # are ignored.

# Dependencies
# This folder contains all third-party libraries. It's generated by 'npm install'
# and should never be committed.
/node_modules

# Environment Variables
# This file contains sensitive information like API keys and database passwords.
# It is specific to each developer's machine and must NEVER be committed.
.env

# Logs
# Log files are often created by applications for debugging. They are transient
# and can grow very large.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS-generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
